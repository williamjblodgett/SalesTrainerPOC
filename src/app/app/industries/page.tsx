import Link from "next/link";

import industryPacks from "../../../../public/data/industry-packs.json";

export default function Page() {
  const total = industryPacks.reduce((sum, pack) => sum + pack.scenarios.length, 0);
  return <><div className="page-header"><div><span className="eyebrow">Curated practice library</span><h1>Seven industries. {total} buyer conversations.</h1><p className="page-lead">Research-informed scenarios combine realistic buyer pressure, responsible-selling guardrails, and observable grading criteria.</p></div><div className="header-actions"><Link className="button" href="/app/practice">Start practice</Link></div></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{industryPacks.map((pack) => <article className="card" key={pack.id}><div className="flex items-start justify-between gap-3"><span className="flex h-12 min-w-12 items-center justify-center rounded-xl text-xs font-black text-white" style={{ backgroundColor: pack.color }}>{pack.icon}</span><span className="badge">5 simulations</span></div><h2 className="mt-5 text-xl font-semibold">{pack.name}</h2><p className="mt-2 min-h-14 text-sm leading-6 text-slate-500">{pack.description}</p><div className="mt-5 border-t border-slate-100 pt-4"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Buyer roles</p><p className="mt-2 text-sm text-slate-700">{pack.buyerRoles.slice(0, 3).join(" · ")}</p></div><details className="mt-4"><summary className="cursor-pointer text-sm font-semibold text-teal-700">View scenarios</summary><ul className="mt-3 space-y-2 text-sm text-slate-600">{pack.scenarios.map((scenario) => <li key={scenario.id}>• {scenario.title} <span className="text-xs text-slate-400">({scenario.difficulty})</span></li>)}</ul></details></article>)}</div></>;
}
