const criteria = [["Opening and agenda", 10], ["Question quality", 15], ["Pain discovery", 20], ["Business-impact discovery", 20], ["Active listening", 15], ["Relevant positioning", 10], ["Next-step control", 10]] as const;

export default function Page() {
  return <><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-teal-600">Manager-owned scoring</p><h1 className="mt-2 text-4xl font-semibold">Discovery scorecard</h1><p className="mt-2 text-slate-600">Score observable behavior—not whether the buyer said yes.</p></div><button className="button">Publish scorecard</button></div>
  <div className="mt-8 grid gap-6 lg:grid-cols-[1fr_300px]"><section className="card"><div className="space-y-3">{criteria.map(([name, weight], index) => <div className="rounded-xl border border-slate-200 p-4" key={name}><div className="flex items-center gap-3"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-slate-100 text-sm font-bold">{index + 1}</span><input defaultValue={name} /><input className="max-w-20" type="number" defaultValue={weight} /><span className="text-sm">%</span></div><details className="mt-3"><summary className="cursor-pointer text-sm font-semibold text-teal-700">Edit 0–4 behavioral anchors</summary><p className="mt-3 text-sm text-slate-500">Every score requires criterion-specific transcript evidence and literal anchor mapping.</p></details></div>)}</div></section>
  <aside className="card h-fit"><p className="text-sm text-slate-500">Total weight</p><p className="mt-1 text-4xl font-semibold">100%</p><div className="mt-5 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-800">Ready to publish · all anchors complete</div><h3 className="mt-6 font-semibold">Scoring rules</h3><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-600"><li>Not applicable weights normalize</li><li>Insufficient evidence produces no score</li><li>AI score is preserved after overrides</li></ul></aside></div></>;
}
