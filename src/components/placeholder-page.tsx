export function PlaceholderPage({title,description}:{title:string;description:string}){return <><h1 className="text-3xl font-semibold">{title}</h1><p className="mt-2 text-slate-600">{description}</p><div className="card mt-8 max-w-3xl"><span className="badge">First milestone</span><h2 className="mt-4 font-semibold">Ready for Supabase data</h2><p className="mt-2 text-sm leading-6 text-slate-600">This surface is routed and styled. Its production query and mutation adapters are tracked in the next implementation slice.</p></div></>}
